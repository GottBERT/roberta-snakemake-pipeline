channels:
  - conda-forge
dependencies:
  - python=3.10
  - rust=1.70.0  # Ensure latest Rust version (1.80.0 has issues)
  - cxx-compiler=1.5.2
  - openssl  # Add OpenSSL support
  - pkg-config  # Helps find OpenSSL paths
  - pip:
      - tokenizers==0.12.1